{
  "name": "mock-dashboard-api",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --local",
    "build": "tsc && esbuild src/index.ts --bundle --platform=browser --format=esm --outfile=dist/index.mjs",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250920.0",
    "esbuild": "^0.23.1",
    "typescript": "^5.9.2",
    "wrangler": "^4.38.0"
  },
  "dependencies": {
    "hono": "^4.9.8"
  }
}
