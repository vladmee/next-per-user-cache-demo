// this is displayed immediately after it attempts to navigate to /dashboard
// showing a nice loading state while the first paint is ready
export default function Loading() {
  return <p>Loading...</p>;
}
